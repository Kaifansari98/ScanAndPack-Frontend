/**
 * colors.ts — FactoryOS design tokens
 * Import from anywhere: import { colors } from "@/theme/colors"
 */
export const headerLabel = {
  APPTITLE:  "Furnix",
} as const;

export type HeaderLabel = typeof headerLabel;